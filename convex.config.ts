// Convex config placeholder for build — keep minimal to avoid runtime type errors
// Removed usage of `defineConfig` to remain compatible with the current convex SDK.

const convexConfig = {};
export default convexConfig;
